<div class="row">
    <div class="col-md-6 mb-6">
        <h1>Menu:</h1>
        <ol>
            {{#each burgers}}
            {{#unless eaten}}
            <li>
            {{name}} <button class="changeEaten" data-id="{{id}}" data-newEat="{{eaten}}" type="submit">Eat It!</button>
            </li>
            {{/unless}}
            {{/each}}
        </ol>
    </div>
    <div class="col-md-6 mb-6">
        <h1>All Gone!</h1>
        <ol>
            {{#each burgers}}
            {{#if eaten}}
            <li>
                {{name}}
            </li>
            {{/if}}
            {{/each}}
        </ol>
    </div>
</div>
<div class='row'>
    <div class="col-md-12">
        <form id="create-form">
        <div class="form-group">
            <label for="newBurger">Your burger isn't here? Let's add one!</label>
            <input type="text" class="form-control" name="" id="burg" aria-describedby="helpId" placeholder="">
            <small id="helpId" class="form-text text-muted">What is your burger called?</small>
        </div>
        <button type="submit">Add That Burger So I Can Eat It!</button>
        </form>
    </div>
</div>